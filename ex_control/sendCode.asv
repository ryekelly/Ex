function sendCode(code)
% function sendCode(code)
%
% simply calls digCode, assuming that the sendingCodes flag is set.

    global params thisTrialCodes

    if params.sendingCodes
        digCode(code);        
    end
    
    trialCodes toc;
end
